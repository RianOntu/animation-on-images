import React from 'react';
import './ZoomOut.css';

const ZoomOut = ({children}) => {
    return (
        
            <div className="zoom-out-container">
      {children}
    </div>
        
    );
};

export default ZoomOut;